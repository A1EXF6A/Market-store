export enum ItemStatus {
  ACTIVE = "active",
  SUSPENDED = "suspended",
  HIDDEN = "hidden",
  PENDING = "pending",
  BANNED = "banned",
}

export enum ItemType {
  PRODUCT = "product",
  SERVICE = "service",
}
